{
  "scripts": {
    "build": "npx tsc && cp manifest.json dist/js && cp ext-stylesheet.css dist/js && cp icon128.png dist/js",
    "publish": "npm run build && cd dist/js && zip Archive.zip *  && cd ../.."
  },
  "devDependencies": {
    "typescript": "^5.0.4"
  }
}
